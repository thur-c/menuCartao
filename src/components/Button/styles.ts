import styled from 'styled-components/native';

export const MainContainer = styled.TouchableOpacity`
  min-height: 100px;
  min-width: 150px;
  padding: 20px;
  align-items: center;
  justify-content: center;
  flex-direction: row;
  border-radius: 28px;
  background-color: #1e40af;
  margin: 12px;
  flex: 1;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Adiciona uma sombra leve */
  elevation: 5;
  border: 1px solid #174096; /* Adiciona uma borda para destacar */

`;
